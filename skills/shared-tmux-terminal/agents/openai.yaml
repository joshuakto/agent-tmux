interface:
  display_name: "Shared Tmux Terminal"
  short_description: "Use the golden path for shared tmux sessions"
  default_prompt: "Use $shared-tmux-terminal to launch a project-local tmux session, report it, send prompts with marks, and read only new output without treating logs as task truth."
